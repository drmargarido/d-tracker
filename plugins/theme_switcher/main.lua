return {
    conf = {
        in_menu = true,
        description = "Themes"
    },

    init = function()
        -- Load last used theme and set it as the current one
    end,

    on_menu_select = function()
        -- Display window with the options to select the theme
    end,

    event_listeners = {}
}
